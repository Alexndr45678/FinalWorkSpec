package animals;

import java.util.List;

public class Cat extends HomeAnimal {
    public Cat(String name, String birthDay, String voice, String color, List<String> commands) {
        super(name, birthDay, voice, color, commands);
    }
}
